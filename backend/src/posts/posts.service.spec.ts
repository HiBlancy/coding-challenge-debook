/**
 * Esqueleto de test unitario para PostsService.
 *
 * Reemplaza los `it.todo` por al menos un test unitario relevante del service.
 * Puedes mockear los repositorios o levantar una DB de test — tú decides y lo
 * justificas.
 */
describe('PostsService', () => {
  it.todo('cubre el comportamiento relevante de like / unlike');
});
