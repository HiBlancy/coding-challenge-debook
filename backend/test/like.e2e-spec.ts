/**
 * Esqueleto de test e2e del endpoint de like.
 *
 * Implementa al menos un test e2e del flujo de like contra la API real
 * (arranca la app con `Test.createTestingModule({ imports: [AppModule] })`,
 * siembra un post y ejercita `POST` / `DELETE /v1/posts/:id/like`).
 * Puedes usar la DB de docker-compose o una DB de test dedicada.
 */
describe('POST /v1/posts/:id/like (e2e)', () => {
  it.todo('cubre el flujo de dar y quitar like');
});
